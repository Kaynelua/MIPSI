SIMULATOR
	- REG
	- MEM



SIMULATOR.LOAD_BIN() - > LOAD BINARY INTO MEMORY

SIMLATOR.RUN()	- > run through the instruction memory
	- instruction = mem.read(pc);
	- decode_control.decode(instruction);	-> state of decoder is : type of instr, list of decoded operands, 
	- 